import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const CommonBottomSheet = () => {
  return (
    <View>
      <Text>CommonBottomSheet</Text>
    </View>
  )
}

export default CommonBottomSheet

const styles = StyleSheet.create({})